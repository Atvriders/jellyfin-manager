services:
  jellyfin-manager:
    build: ./app
    ports:
      - "5000:5000"
    environment:
      - JELLYFIN_URL=${JELLYFIN_URL}
      - JELLYFIN_API_KEY=${JELLYFIN_API_KEY}
    restart: unless-stopped
